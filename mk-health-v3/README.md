# mk-health-v3

Professional health tracker scaffold with labs, glucose, medications, doctors, appointments, goals, AI insights, and notifications.

## Setup

1. Install dependencies: `npm install`
2. Run development server: `npm run dev`
3. Open `http://localhost:3000`
