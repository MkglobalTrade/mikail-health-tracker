# mk-health-v3

Professional health tracker scaffold with labs, glucose, medications, doctors, appointments, goals, reports, AI insights, and settings.

## Setup

1. Copy `.env.example` to `.env.local` and add your API keys.
2. Install dependencies: `npm install`
3. Run the app: `npm run dev`
